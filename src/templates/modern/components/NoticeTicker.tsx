import React, { useEffect, useState } from 'react';
import { Pause, Play } from 'lucide-react';
import { Link } from '@tanstack/react-router';
import { notices } from "#content/site";
export default function NoticeTicker(){const [i,setI]=useState(0);const [paused,setPaused]=useState(false);useEffect(()=>{if(paused)return;const id=setInterval(()=>setI(x=>(x+1)%notices.length),5000);return()=>clearInterval(id)},[paused]);const n=notices[i];return <section className="ticker" onMouseEnter={()=>setPaused(true)} onMouseLeave={()=>setPaused(false)} onFocus={()=>setPaused(true)} onBlur={()=>setPaused(false)} aria-label="School notices" data-testid="notice-ticker"><span className="ticker-label">NOTICE</span><ul><li data-testid="active-notice-title">{n.title}</li>{notices.filter(x=>x.id!==n.id).map(x=><li className="sr-only" key={x.id}>{x.title}</li>)}</ul><time data-testid="active-notice-date">{n.publishedAt}</time><button onClick={()=>setPaused(!paused)} aria-label={paused?'Play notices':'Pause notices'} data-testid="notice-pause-button">{paused?<Play/>:<Pause/>}</button><Link to="/contact" data-testid="notice-view-all-link">View all</Link></section>}