const fs = require("fs"), os = require("os"), path = require("path"); const dir = path.join(os.homedir(), ".config", "opencode"); fs.mkdirSync(dir, { recursive: true }); fs.writeFileSync(path.join(dir, "opencode.json"), JSON.stringify({ model: "omniroute/auto", smallModel: "omniroute/auto", provider: { omniroute: { npm: "@ai-sdk/openai-compatible", name: "OmniRoute", options: { baseURL: "http://127.0.0.1:20128/v1", apiKey: "sk-28db7b160f7b5252-94baa2-f2693d4b" }, models: { auto: { name: "auto", limit: { context: 128000, input: 128000, output: 4096 } } } } } }, null, 2));
